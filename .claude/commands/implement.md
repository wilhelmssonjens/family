---
description: Implementera ett specifikt steg från en plan
---

Implementera: $ARGUMENTS

Regler:
1. Gör minimala ändringar — bara det som krävs för detta steg
2. Kör tester efter ändringen: `npx vitest run`
3. Kör build: `npm run build`
4. Committa inte automatiskt — visa diffen först
5. Om det påverkar UI, verifiera visuellt med Playwright om möjligt
